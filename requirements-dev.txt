# Development-only tooling (not required to run the pipeline).
# Install with: pip install -r requirements-dev.txt
pytest>=8.0
ruff>=0.6

# Visualizations (app/generate_visualizations.py)
matplotlib>=3.7
pandas>=2.0
seaborn>=0.12
